version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/springboot-dev  # EC2 배포 경로

hooks:
  ApplicationStop:
    - location: scripts/stop.sh
      timeout: 30
      runas: ubuntu

  ApplicationStart:
    - location: scripts/start.sh
      timeout: 180
      runas: ubuntu
